apiVersion: 1

datasources:
  - uid: prometheus
    name: Prometheus
    type: prometheus
    access: proxy
    # use environment variable so the same file works outside Docker
    # (defaults to localhost:9090).
    url: "${GF_DATASOURCE_PROMETHEUS_URL:http://localhost:9090}"
    isDefault: true
    editable: true
